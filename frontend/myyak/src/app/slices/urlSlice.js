import { createSlice } from "@reduxjs/toolkit";

const urlSlice = createSlice({
  name: "url",
  initialState: {
    url: "https://i10a201.p.ssafy.io/api"
  },
  reducers: {}
})

export default urlSlice;